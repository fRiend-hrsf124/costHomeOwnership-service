/* eslint-disable no-unused-vars */
import React from 'react';
import { shallow } from 'enzyme';
import RatesFooter from './RatesFooter';

describe('RatesFooter', () => {
  test.skip('It should make actual tests', () => {
    expect(true).toBe(true);
  });
});
