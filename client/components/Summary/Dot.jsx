/* eslint-disable import/extensions */
import React from 'react';
import { } from '../styles.jsx';

const Dot = (props) => {
  const {
    color,
  } = props;
  return (
    <>
      {color}
    </>
  );
};

export default Dot;
