/* eslint-disable no-unused-vars */
import React from 'react';
import { shallow } from 'enzyme';
import Rates from './Rates';

describe('Rates', () => {
  test('It should make actual tests', () => {
    expect(true).toBe(true);
  });
});
